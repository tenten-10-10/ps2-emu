{
  "name": "ps2-emu-teaser",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:preview": "PS2_SITE_URL=https://ps2-emu.example PS2_SITE_BASE_PATH= node scripts/build.mjs",
    "test": "npm run test:preview && npm run test:production",
    "test:preview": "PS2_SITE_URL=https://ps2-emu.example PS2_SITE_BASE_PATH= npm run build && PS2_SITE_URL=https://ps2-emu.example PS2_SITE_BASE_PATH= node scripts/test.mjs",
    "test:production": "npm run build && node scripts/test.mjs",
    "preview": "python3 -m http.server 4173 --directory dist"
  },
  "engines": {
    "node": "24.x"
  }
}
